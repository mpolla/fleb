// Conventional Commits ruleset, read by the commitlint CI check on PRs.
// See AGENTS.md "Commit conventions" for the human-readable version.
module.exports = {
    extends: ['@commitlint/config-conventional'],
};
